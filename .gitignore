# Dependencies
node_modules/
.pnp/
.pnp.js

# Secrets / environment — NEVER commit (competition DQ rule)
.env
.env.*
!.env.example
*.pem
*.key
secrets.json

# Build output
dist/
build/
out/
.next/
.turbo/
.cache/

# Smart-contract tooling (Hardhat / Foundry)
artifacts/
cache/
typechain/
typechain-types/
coverage/
coverage.json
broadcast/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Internal / private notes — never push to GitHub (concept, rules, strategy, orientation)
surfstyk-notes/
CLAUDE.md

# Settlement backend local state
server/.settled/
server/plan.json

# Local SQLite database (shared-state API)
*.db
*.db-shm
*.db-wal
